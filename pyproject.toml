[project]
name = "cybersentinel-ai-dashboard"
version = "0.1.0"
description = "Cybersecurity AI Dashboard"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "openai",
    "pydantic",
    "openenv>=0.1.13"   # ✅ REQUIRED FIX
]

[tool.uv]
